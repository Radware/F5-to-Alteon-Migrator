# Fixtures and golden files are compared byte-for-byte by the test suite.
# Force LF on checkout everywhere so Windows clones (core.autocrlf=true)
# don't rewrite them to CRLF and break the golden tests.
* text=auto eol=lf
*.docx binary
*.pdf binary
*.exe binary
*.png binary
